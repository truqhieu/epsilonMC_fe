import styled from "styled-components";

export const InfoRowStyled = styled.div`
  .info-row {
    display: flex;
    flex-direction: row;
    gap: 100px;
  }

  .info-row-label {
    width: 90px;
  }

  .info-row strong {
    min-width: 135px;
  }
`;
